// Re-export so the requested file layout (pages/ProductsPage.jsx) is honored
// without duplicating the section's implementation.
import FeaturedProducts from '../sections/FeaturedProducts';
export default FeaturedProducts;
