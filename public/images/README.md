# Drop your 10 vegetable images here

The site expects the following filenames, copied from the parent `veg fresh/` folder:

```
image.png     (used as hero + featured product)
image-2.png
image-3.png
image-4.png   (also used as About section image)
image-5.png
image-6.png
image-7.png
image-8.png
image-9.png
```

After copying, run `npm install && npm run dev` from the `veggie-fresh/` directory.